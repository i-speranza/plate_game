services:
  - type: web
    name: plate-game
    runtime: node
    region: frankfurt
    plan: free
    branch: main
    buildCommand: npm install && npm run build:prod
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: "20"
