{
  "name": "plate-game",
  "private": true,
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w client",
    "build:prod": "npm run build -w shared && npm run build -w server && npm run build -w client",
    "build:dicts": "npm run build:dicts -w server",
    "start": "node server/dist/index.js",
    "test": "npm run test -w shared"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
